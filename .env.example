DB_HOST=103.13.112.90
DB_PORT=3306
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password

ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=replace_with_bcrypt_hash
ADMIN_SESSION_SECRET=replace_with_long_random_secret

# Production domain(s) that may submit admin Server Actions.
# Use hostnames only or full URLs; comma-separate multiple domains.
ADMIN_ALLOWED_ORIGINS=example.com,www.example.com

# Optional local-only fallback for setup. Prefer ADMIN_PASSWORD_HASH in production.
# ADMIN_PASSWORD=change-this-password
